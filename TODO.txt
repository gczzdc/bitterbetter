CSS / HTML
change tooltips to (javascript?) box
add high effect wordlist
add examples section to intro

FLASK BACKEND
deploy to heroku

ML BACKEND
try word2vec
try lemmatization
try log weighting
build another regressor with no style

BACKEND EVENTUAL
overtune hyperparameters
build feature_coef_dic from fully fitted regressor

WISHLIST:
save common styles in a json file instead of hard coding them
clean CSS
move css out of html document